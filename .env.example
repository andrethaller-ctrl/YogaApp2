# Supabase — use DEV project credentials for local development only.
# Copy to .env and fill in values from Supabase Dashboard → Settings → API.
# Never commit .env (listed in .gitignore).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
